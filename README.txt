THE GULAG — All-in-One + Brand (Single ZIP)
-------------------------------------------
1) Upload ALL files to your GitHub repo (root level).
2) Deploy on Render (Build: npm install, Start: node app.js, Health: /health).
3) Visit your site → icons & PWA are already wired (no edits).

Pages:
- /            → THE GULAG dashboard (branded, installable PWA)
- /health      → health check JSON
- /store.html  → Rewards store placeholder
- /topup.html  → Buy points placeholder

Icons/Manifest:
- Already included and linked in each page’s <head>.
- If you don’t see the new icon on phone, remove old home-screen shortcut and add again.
